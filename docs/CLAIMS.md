# Claim ledger — all-schedule revision

| Claim | Scope and proof | Explicit non-claim |
|---|---|---|
| Balanced static mixture is globally best | Common-diagonal symmetric generators and diagonal initial second moment; spectral Jensen | Not arbitrary data mixtures |
| Exact boundary epsilon = exp(-2 B cos(phi)) | Main Theorem 2; `global_proofs.tex`; every measurable precommitted schedule in the mirror-source family | Not feedback schedules observing realized error; not arbitrary target geometry |
| Below the boundary two mixed phases improve strictly | Explicit half-budget perturbation with first-order column-inner-product cancellation | The finite-amplitude witness is not globally optimal at all budgets |
| Fixed epsilon gives eventual exact static optimality | Rearrangement of the boundary; any number of phases | Requires fixed positive epsilon and stated geometry |
| Near-boundary gain coefficient is 1/8 | Gram lower envelope and a two-phase witness matching to cubic order | Constants are not uniform as geometry or budget degenerates |
| Additive noise has a balanced lower contribution | Isotropic Brownian forcing, deterministic path-independent schedule; suffix argument | Not an assertion that SGD equals this diffusion; boundary only sufficient with noise |
| Pure preparation is globally rate-optimal at zero uncertainty | Energy lower bound and explicit construction | Not exact finite-budget optimality or an unbounded compute-to-target benefit |
| Shared schedule cannot improve spectral exponent | Uniform block rate envelope and one uniformly adequate static mixture | Not general feature learning; no adaptive capacity growth |
| Exact expected SGD | Fresh Gaussian examples, independent source sampling and label noise; explicitly distinguishes per-example and per-batch domains | Not finite-dataset SGD or universal neural-network dynamics |
| Global static SGD bracket | Ordered-product curvature plus scalar interpolation | Float64 evaluation is not directed-rounding formal verification |
| 186/432 primary settings exceed 1% | Full fixed condition grid; comparison with lower static bracket | Oracle moments and search are uncharged; candidates include static |
| Paid pilot gate | Uniform confidence event plus full-cost benchmark | Rejected pilots still cost work; buying the pilot is not recommended by the theorem |

All theorem proofs are supplied. Internal proof checking and numerical tests are AI-assisted, not independent review. The previous two-phase-only limitation is closed for the main mirror-source model; it remains a limitation of the generic Golden–Thompson corollary for unrelated matrices.
